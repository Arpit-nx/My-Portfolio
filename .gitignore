# === Node / Frontend ===
/node_modules/
/dist/
/dist-ssr/
/*.local         # local override files like foo.local if you use that pattern

# Logs (frontend or backend)
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# === Python / Backend ===
# Bytecode, caches
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
/.venv/
/env/
/venv/
/ENV/

# Flask / Django local artifacts
/instance/
local_settings.py
db.sqlite3
*.db
*.sqlite3
/media/

# Testing / coverage / type checking
.coverage
.coverage.*
.cache
.pytest_cache/
htmlcov/
.mypy_cache/
.dmypy.json
dmypy.json

# Distribution / packaging
/build/
/dist/                # already in frontend, okay to keep once (no harm)
/*.egg-info/
.eggs/

# === Secrets / env ===
.env                 # real secret files (should never be committed)
# keep a committed example instead: .env.example

# === Editors / IDEs ===
.vscode/*
!.vscode/extensions.json
.idea/
/*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === OS / Misc ===
.DS_Store
*.local
